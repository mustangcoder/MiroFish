[project]
name = "mirofishplus-chatgpt-oauth-gateway"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = ["flask>=3.1,<4", "gunicorn>=23,<24", "httpx>=0.28,<1", "openai>=1.109,<2"]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
